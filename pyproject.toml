[project]
name = "yang2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "oasis-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
oasis-tools = { workspace = true }
